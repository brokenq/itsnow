-var readonly = true
//- include the form mixins
include /lib/mixins/form

h2 处理 故障单

+form("actionIncidentForm")(ng-submit="action()")
  include /msp/index/incidents/_form.jade

  +action_bar
    button.btn.no-border(class="btn-primary",type=submit)
      i.bigger-110(class="icon-ok")
      | {{buttonLabel}}
    +action_cancel("取消")(ui-sref="incidents.opened")
