-var readonly = false
//- include the form mixins
include /lib/mixins/form

h2 新增 故障单

+form("newIncidentForm")(ng-submit="create()")
  include /msp/index/incidents/_form.jade

  +action_bar
    +action_submit("创建")
    +action_cancel("取消")(ui-sref="incidents.opened")
