-var readonly = true
-var disabled_type = "host.type"
//- include the form mixins
include /lib/mixins/form

h2 查看 主机

include /msc/index/hosts/_form.jade

+section("开通记录")
  +area("host", "creationLog")(readonly=readonly, rows="20", style="overflow-y:visible")
+action_bar
  +action_submit("返回")(ui-sref="hosts.list")