-var readonly = false

h2 新增用户

form.form-horizontal(name="newUserForm", novalidate="novalidate", ng-model-options="{ updateOn: 'mousedown blur' }", role="form", ng-submit="create()")
  include /msc/index/system/users/_form.jade
  +action_bar
    +action_submit("创建")
    +action_cancel("取消")(ui-sref="users.list")