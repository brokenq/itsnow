mixin blocks(title)
  h2 #{title}
  hr
  block
mixin formGroup()
  .form-group
    .col-md-offset-2.col-md-8
      block
mixin label(title)
  label.control-label.col-md-2 #{title}:
  .col-md-8
    block
mixin formField(label, value)
  label.control-label.col-md-2 #{label}:
  div(class!=attributes["class"])
    input.form-control(type="text", ng-model="#{value}", readonly)

mixin tableForm(auth)
  tr
    td #{auth}






.form-horizontal(ui-view ng-controller='egCtrl')
  +blocks("组详情")
    +formGroup()
      +formField("SN", "xxxx")(class="col-md-3")
      +formField("组名称", "xxxx")(class="col-md-3")
    +formGroup()
      +formField("创建时间", "xxxx")(class="col-md-3")
      +formField("更新时间", "xxxx")(class="col-md-3")
    +formGroup()
      +label('组描述')
        textarea.form-control(rows='4',readonly)

    +formGroup()
      +label('权限')
        table.table.table-striped.table-bordered.table-condensed
          tr
            th 名称
          +tableForm('xxxx')
          +tableForm('xxxx')
          +tableForm('xxxx')

    +formGroup()
      +label('用户')
        table.table.table-striped.table-bordered.table-condensed
          tr
            th 用户名
          +tableForm('xxxx')



























      //.col-md-4.col-md-offset-4
      //  ul.pagination
      //    +button("#", "《")
      //    +button("#","2")
      //    +button("#","3")
      //    +button("#","4")
      //    +button("#","5")
      //    +button("#","》")