-var readonly = false

h2 新增 进程

form.form-horizontal(name="newProcessForm", novalidate="novalidate", ng-model-options="{ updateOn: 'mousedown blur' }", role="form", ng-submit="create()")
  include /msc/index/processes/_form.jade

  +action_bar
    +action_submit("创建")
    +action_cancel("取消")(ui-sref="processes.list")