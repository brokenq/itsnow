-var readonly = false
-var editReadonly = false
//- include the form mixins
include /lib/mixins/form
h2 新增 服务项

form("newItemForm")(ng-submit="create()")
  include /msc/index/service/catalog/_form_item.jade

  +action_bar
    +action_submit("创建")
    +action_cancel("取消")(ui-sref="catalog.list")
