-var readonly = false
//- include the form mixins
include /lib/mixins/form

h2 新增 数据库

+form("newSchemaForm")(ng-submit="create()")
  include /msc/index/schemas/_form.jade

  +action_bar
    +action_submit("创建")
    +action_cancel("取消")(ui-sref="schemas.list")
